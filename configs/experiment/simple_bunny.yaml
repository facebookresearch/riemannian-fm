# @package _global_
data: simple_bunny
use_wandb: True

model:
  d_model: 512
  num_layers: 6
  atol: 1e-4
  rtol: 1e-4

optim:
  num_iterations: 50000
  batch_size: 400
  val_batch_size: 20000
  lr: 0.0001
  wd: 0.001

val_every: 2000
ckpt_every: 50
early_stopping_patience: 4
